/**
 * Stub temporário. Assim que o projeto Supabase existir (ver SETUP.md),
 * gere os tipos reais com:
 *
 *   npx supabase gen types typescript --project-id SEU_PROJECT_ID > src/types/supabase.ts
 *
 * e este arquivo passa a refletir exatamente supabase/migrations/0001_init.sql.
 */
// oxlint-disable-next-line no-explicit-any
export type Database = any
