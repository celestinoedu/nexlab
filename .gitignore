# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Relatórios de exemplo dos parceiros: contêm nomes de pacientes de terceiros.
# Repositório é público — mantidos só localmente como referência, nunca versionados.
docs/assets/relatorios-exemplo/

# NUNCA versionar: senha do banco, service_role key, anon key etc.
# Repositório é público — isso teria que ser rotacionado imediatamente se vazasse.
Supabase Credentials/
*credentials*.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
