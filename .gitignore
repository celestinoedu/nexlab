# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Relatórios de exemplo dos parceiros: contêm nomes de pacientes de terceiros.
# Repositório é público — mantidos só localmente como referência, nunca versionados.
docs/assets/relatorios-exemplo/

# PDF original da lista de preços (asset binário) — os dados já estão em
# supabase/seed.sql (versionado, com aprovação do cliente); o PDF em si fica
# só localmente por higiene de repositório (asset binário de referência).
docs/assets/lista-preco/

# NUNCA versionar: senha do banco, service_role key, anon key etc.
# Repositório é público — isso teria que ser rotacionado imediatamente se vazasse.
Supabase Credentials/
*credentials*.txt

# Scripts temporários de setup (podem conter a service_role key inline) — nunca versionar.
.tmp-*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Artefatos locais de validação visual e pacote compactado para entrega.
.playwright-cli/
output/
NexLab-Brand-Pack-*.zip
